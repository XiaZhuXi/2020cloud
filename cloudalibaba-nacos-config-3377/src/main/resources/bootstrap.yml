server:
  port: 3377
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: ad43fec6-d629-4bac-a9f0-45d619c805bd
        group: DEV_GROUP
